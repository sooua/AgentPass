# README screenshots

Drop PNGs here with these exact names — the root `README.md` / `README.zh-CN.md`
reference them. Until they exist, the image slots render broken on GitHub.

| File | Shot | Suggested width |
|------|------|-----------------|
| `dashboard.png` | Desktop app home / targets overview (the hero image) | ~1640px (2×) |
| `agent-tokens.png` | Settings → Agent tokens, with the "Recommended agent token" preset | ~1000px |
| `checkout.png` | A checkout result showing the expiring `ssh` command | ~1000px |
| `approvals.png` | The Approvals page with a pending reveal request | ~1000px |
| `sync.png` | The sync modal (provider + encryption state) | ~1000px |
| `audit.png` | The audit log table (optional, not yet referenced) | ~1000px |

Tips: capture on a clean window at 2× scale, trim OS chrome, keep a consistent
theme across shots. Use fake/sample data — never real hosts or secrets.
